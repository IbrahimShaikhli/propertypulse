// constants.dart

import 'package:flutter/material.dart';

// Define your purple color constants
const Color primaryColor = Color(0xFF9C27B0);   // A primary purple color
const Color accentColor = Color(0xFFE040FB);    // A lighter shade of purple for accents
const Color backgroundColor = Color(0xFFF5F5F5); // A light gray background
const Color textColor = Color(0xFF333333);      // A dark gray text color
const Color buttonColor = Color(0xFF673AB7);    // A slightly darker shade of purple for buttons
const Color errorColor = Color(0xFFE57373);     // A red error color
